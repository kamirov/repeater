{
  "name": "service-state",
  "version": "0.0.1",
  "description": "",
  "main": "src/app.ts",
  "keywords": [],
  "author": "kamirov",
  "license": "UNLICENSED",
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^16.11.2",
    "rimraf": "^3.0.2",
    "tsconfig-paths": "^3.11.0",
    "typescript": "^4.4.4",
    "aws-sdk": "^2.1133.0"
  },
  "scripts": {
    "build": "rimraf ./build && tsc",
    "postbuild": "cp package.json build/package.json && cd build && yarn --prod"
  }
}
